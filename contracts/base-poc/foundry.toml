[profile.default]
src = "src"
out = "out"
test = "test"
libs = ["lib"]
solc = "0.8.28"
optimizer = true
optimizer_runs = 200
# Keep the POC isolated; it must never pull in or disturb the Solana workspaces.
